import { WorkspaceSectionPage } from "@/components/workspace/workspace-section-page";

export default function CalendarBravoPage() {
  return (
    <WorkspaceSectionPage
      title="Calendar — Bravo"
      tagline="Dummy page"
      description="Placeholder sub-page under Calendar. We’ll replace this with the real destination later."
    />
  );
}
